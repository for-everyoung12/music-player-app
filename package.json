{
  "name": "music-player",
  "version": "1.0.0",
  "description": "Simple Electron music player",
  "main": "main.js",
  "scripts": {
  "start": "electron .",
  "build": "electron-packager . music-player --platform=win32 --arch=x64 --icon=assets/icons/icon.ico --overwrite"
  },
  "devDependencies": {
    "electron": "^35.0.3",
    "electron-packager": "^17.1.2",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
